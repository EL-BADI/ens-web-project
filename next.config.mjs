/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "img.freepik.com",
      "img.icons8.com",
      "lh3.googleusercontent.com",
    ],
  },
};

export default nextConfig;
